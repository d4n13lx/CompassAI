# Copie para `.env` na mesma pasta (raiz do projeto). O Prisma CLI só carrega `.env`, não este arquivo.
#
# MongoDB Atlas (ou Mongo local):
# DATABASE_URL="mongodb+srv://USUARIO:SENHA@cluster0.xxxxx.mongodb.net/NOME_DO_DB?retryWrites=true&w=majority"
DATABASE_URL=

# Token simples para /admin e APIs de moderação (header x-moderator-token)
MODERATOR_TOKEN=
